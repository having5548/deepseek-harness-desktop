<Window
    x:Class="DshDesktop.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部工具栏 -->
        <Border Grid.Row="0" Background="{ThemeResource LayerFillColorDefaultBrush}"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="10,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" Spacing="4" HorizontalAlignment="Left">
                    <Button x:Name="BackButton" Click="BackButton_Click" ToolTipService.ToolTip="后退" Padding="8,4">
                        <FontIcon Glyph="&#xE72B;" FontSize="14" />
                    </Button>
                    <Button x:Name="ForwardButton" Click="ForwardButton_Click" ToolTipService.ToolTip="前进" Padding="8,4">
                        <FontIcon Glyph="&#xE72A;" FontSize="14" />
                    </Button>
                    <Button x:Name="ReloadButton" Click="ReloadButton_Click" ToolTipService.ToolTip="重新加载" Padding="8,4">
                        <FontIcon Glyph="&#xE72C;" FontSize="14" />
                    </Button>
                    <Button x:Name="ExternalButton" Click="ExternalButton_Click" ToolTipService.ToolTip="在系统浏览器中打开" Padding="8,4">
                        <FontIcon Glyph="&#xE8A7;" FontSize="14" />
                    </Button>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
                    <TextBlock x:Name="ServiceStateText" Text="服务未运行" FontSize="12" Opacity="0.7"
                               VerticalAlignment="Center" Margin="0,0,8,0" />
                    <Button x:Name="SettingsButton" Click="SettingsButton_Click" ToolTipService.ToolTip="设置" Padding="8,4">
                        <FontIcon Glyph="&#xE713;" FontSize="14" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 内容区：WebView2 + 状态覆盖层 -->
        <Grid Grid.Row="1">
            <WebView2 x:Name="WebView" />
            <Grid x:Name="StatusOverlay" Background="{ThemeResource SolidBackgroundFillColorBaseBrush}" Visibility="Collapsed">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12" MaxWidth="520">
                    <ProgressRing x:Name="StatusRing" IsActive="True" Width="40" Height="40" HorizontalAlignment="Center" />
                    <TextBlock x:Name="StatusText" Text="正在初始化…" TextAlignment="Center" TextWrapping="Wrap" Style="{ThemeResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Name="StatusDetail" FontSize="12" Opacity="0.7" TextAlignment="Center" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Window>
